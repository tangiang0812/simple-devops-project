apiVersion: v1
kind: Service
metadata:
  name: ops-inspiration-console-service
  namespace: ops-inspiration-console
spec:
  type: LoadBalancer
  selector:
    app: ops-inspiration-console-app
  ports:
    - port: 80
      targetPort: 8080
